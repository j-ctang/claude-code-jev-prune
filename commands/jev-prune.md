---
description: Prune stale tool results from context now, even below the automatic threshold (jev-prune proxy)
---

The jev-prune proxy recognizes this command and prunes stale tool results from the conversation as this message is sent. Report the `[jev-prune]` notice attached to this message in one short line. If no `[jev-prune]` notice is attached, say the jev-prune proxy is not running or `ANTHROPIC_BASE_URL` does not point at it.
